# OSM Station
Tile server and generation worker using Planetiler and Martin.

## Usage
1. Place .osm.pbf in `data/`
2. Run `./scripts/generate_map.sh`
3. Start server with `./bin/martin data/yourmap.pmtiles`

